# @package _global_
model:
  # any link that is not specified as learnable will be initialized from urdf
  rel_urdf_path: "fetch_description/urdf/fetch_arm.urdf"
  learnable_rigid_body_config:
    learnable_links: []
  name: "diff_fetch_robot_model_gt"
